# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to your Supabase database.
POSTGRES_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"

# You can generate the secret via 'openssl rand -base64 32' on Unix.
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='<YOUR_AUTH_SECRET>'

# Optional comma-separated extra origins allowed by Better Auth.
# Useful for local iOS testing over LAN (for example, http://192.168.4.101:3000).
AUTH_DEV_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,http://192.168.4.101:3000"

# Optional canonical HTTPS URL used for OAuth provider callbacks/proxying.
# Set this to your production domain when developing via localhost/LAN so providers like Google don't reject private IP callbacks.
AUTH_DEV_PROXY_URL="https://<YOUR_PRODUCTION_WEB_DOMAIN>"

# Pre-configured Google OAuth provider, works out-of-the-box.
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID='<AUTH_GOOGLE_ID>'
AUTH_GOOGLE_SECRET='<AUTH_GOOGLE_SECRET>'

# Pre-configured GitHub OAuth provider, works out-of-the-box.
# @see https://www.better-auth.com/docs/authentication/github
AUTH_GITHUB_ID='<AUTH_GITHUB_ID>'
AUTH_GITHUB_SECRET='<AUTH_GITHUB_SECRET>'

# OpenAI Key for vector embeddings & editor chat completions.
OPENAI_API_KEY="<OPENAI_API_KEY>"

# Public URL of the web app. In preview URLs it should be `PUBLIC_WEB_URL=https://$NEXT_PUBLIC_VERCEL_URL`.
PUBLIC_WEB_URL="<YOUR_PUBLIC_WEB_URL>"

# Stripe credentials for billing.
# You will need the Stripe CLI for local webhooks: https://docs.stripe.com/stripe-cli
# Grab the API keys from here: https://dashboard.stripe.com/<account_id>/test/apikeys
STRIPE_SECRET_KEY="<STRIPE_SECRET_KEY>"
STRIPE_WEBHOOK_SECRET="<STRIPE_WEBHOOK_SECRET>"
STRIPE_WEBHOOK_PORT="<STRIPE_WEBHOOK_PORT>" # Only needed for local Stripe CLI forwarding; defaults to 3000 in apps/stripe/src/env.ts if omitted.
