{
  "name": "@acme/localtunnel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "biome check",
    "clean": "git clean -xdf .cache .next .turbo node_modules tsconfig.tsbuildinfo",
    "dev": "NODE_ENV=development pnpm with-env pnpm watch",
    "typecheck": "tsc --noEmit",
    "watch": "pnpm with-env tsx watch ./src/main.ts",
    "with-env": "dotenv -e ../../.env --"
  },
  "devDependencies": {
    "@acme/db": "workspace:*",
    "@acme/tsconfig": "workspace:*",
    "@types/express": "~5.0.6",
    "@types/localtunnel": "~2.0.4",
    "@types/node": "~25.2.3",
    "concurrently": "~9.2.1",
    "dotenv-cli": "~11.0.0",
    "express": "~5.2.1",
    "localtunnel": "~2.0.2",
    "tsx": "~4.21.0",
    "zod": "~4.3.6"
  }
}
